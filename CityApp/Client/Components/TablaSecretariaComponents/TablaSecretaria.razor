<div class="content_table">
    <div class="content_title">
        <h2 class="title">@ViewString.Keys["secretariasLis"]</h2>
        <div class="btn_cirquel" @onclick="IrNuevaSecretaria">
            <i class="fas fa-plus"></i>
        </div>
    </div>
    <div class="content_grid_tabla">
        <div class="content_grid_tabla_header">
            <div class="col_grid displa_flex">
                <span>ID</span>
            </div>
            <div class="col_grid displa_flex">
                <span>Secretaría</span>
            </div>
            <div class="col_grid">
                <span>Acciones</span>
            </div>
        </div>
        @if (banderaLoader)
        {
            @if (Secretarias != null)
            {
                @if (Secretarias.Count > 0)
                {
                    <div class="content_grid_tabla_boddy">
                        @foreach (var secretaria in Secretarias)
                        {
                            <div class="col_grid">
                                <p class="txt_plano">@secretaria.IdSecretaria</p>
                            </div>
                            <div class="col_grid">
                                <p class="txt_plano">@secretaria.NombreSecretaria</p>
                            </div>
                            <div class="col_grid">
                                <div class="content_flex">
                                    <button class="btn_edit" @onclick="(() => IrEditarSecretaria(secretaria.IdSecretaria))">
                                        <i class="far fa-edit"></i>
                                        <span>Editar</span>
                                    </button>
                                    <button class="btn_delete" @onclick="(() => EliminarSecretaria(secretaria.IdSecretaria))">
                                        <i class="far fa-trash-alt"></i>
                                        <span>Eliminar</span>
                                    </button>
                                </div>
                            </div>
                        }
                    </div>
                    <div class="content_paginado">
                        @foreach (int i in paginas)
                        {
                            <div class="pagina" @onclick="(() => CambiarPaginaActual(i))">
                                @if (i == paginaActual)
                                {
                                    <span class="selected">@i</span>
                                }
                                else
                                {
                                    <span>@i</span>
                                }
                            </div>
                        }
                    </div>
                }
                else
                {
                    @if (alerta != "")
                    {
                        <h2 class="no_data">No hay Secretarias</h2>
                    }
                    else
                    {
                        <div class="loader"></div>
                    }
                }
            }
            else
            {
                @if (alerta != "")
                {
                    <h2 class="no_data">No hay Secretarias</h2>
                }
                else
                {
                    <h2 class="no_data">@alerta</h2>
                }
            }
        }
        else
        {
            <div class="loader"></div>
        }
    </div>
</div>
